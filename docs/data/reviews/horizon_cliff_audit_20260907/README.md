# Future20 t+19 / t+20 Cliff Audit

1. Checkpoints / SHA: official bare CNO uses `sim_pretrain/sim_cno.pth` (`af85374bfd06c0e386ec803d777396c21484978392213025697c5a7470106b6b`); P0-A CNO uses validation continuation `model_last.pth`, update 30,900 (`e3d5faaf1a71e121b09077dd7dd7d0456a617e2916b8a671986f412fb54f6388`). Frozen manifest SHA is `42b710cb8f04e5ab020da2b69772980b563dcc3f3ad555c21508ab12ab10c347`.
2. The replay used the same frozen 50 train / 16 dev trajectories and exactly 659 fixed-stride dev windows. All output tensors were asserted as `(659, 20, 32, 64, 3)`. For every window, `target == trajectory[start+20:start+40]` for u/v: t+18/t+19/t+20 map to raw frames `start+37`/`start+38`/`start+39` (t+1 is `start+20`).
3. t+18 / t+19 / t+20 window-wise velocity Rel-L2: PERSIST `0.15186532 / 0.15287980 / 0.15355751`; official bare CNO `2.04207373 / 2.28832126 / 1.76658928`; P0-A CNO `0.11934058 / 0.15308243 / 0.17540927`. Ratios t19/t18, t20/t18, t20/mean(t1:t18) are respectively PERSIST `1.00668 / 1.01114 / 1.19206`, official `1.12059 / 0.86510 / 0.61419`, P0-A `1.28274 / 1.46982 / 1.67748`.
4. Cliff assessment: PERSIST has no t19/t20 cliff; official bare CNO has no t19/t20 cliff (its t19 rise reverses at t20); P0-A CNO has a clear late increase, +28.3% at t19 and +47.0% at t20 versus t18. `horizon_metrics.csv` is computed independently as per-window Rel-L2 then mean, matching the stated intended horizon semantics; `analyze_hybrid_cno_local.py` instead applies its `error()` once to the concatenated horizon tensor, so it is a different aggregation and cannot be numerically equated without changing that tool.
5. CNO temporal-dimension audit: the official `CNO3d` uses `Conv3d(kernel_size=3, padding=1)` in lift, encoder/decoder, residual, and projection blocks, so temporal convolution padding is symmetric. With the default `LeakyReLU` activation, there is no temporal downsampling, upsampling, interpolation, crop, or output resize: the `in_size`/encoder-size machinery configures spatial filter sizes only. The final `out_dim_mult` reshape changes channel/time packing only when requested; this model uses `out_dim_mult=1`. No explicit Future20 end-boundary asymmetric implementation was found.
6. Final determination: `P0A_ADAPTATION_SUSPECT`.
7. Next steps: (a) replay the same frozen P0-A checkpoint through the historical evaluation entry point and compare its per-window metric to this independent implementation; (b) compare pre-P0-A and post-P0-A checkpoints with the same script to isolate feature adaptation versus later fine-tuning; (c) inspect per-window t19/t20 residual maps only, without changing training or model code.
